# نسخه 2.4.0

این نسخه تجربه فعال‌سازی و استفاده روزانه از سلف را بازطراحی می‌کند و
با ساختار Watchdog نسخه‌های 2.3.2 و 2.3.3 سازگار است.

## تغییرهای اصلی

- دکمه رسمی ارسال شماره به مرحله فعال‌سازی اضافه شد.
- شماره Contact فقط برای صاحب همان حساب پذیرفته می‌شود.
- درخواست شماره در گروه انجام نمی‌شود و کاربر به پیوی ربات هدایت می‌شود.
- دستور «تنظیم دوست» با ریپلای روی پیام کاربر اضافه شد.
- دوست ثبت‌شده روی پیام‌های متنی پاسخ‌های صمیمی و متنوع دریافت می‌کند.
- پاسخ صمیمی برای بات‌ها، پیام‌های بدون متن و یک پیام تکراری اجرا نمی‌شود.
- منشی پس از پاسخ صمیمی دوست، پاسخ دوم و هم‌زمان ارسال نمی‌کند.
- صفحه دوست و دشمن دارای کلید مستقل برای این قابلیت است.
- راهنما به چهار صفحه موضوعی تقسیم شد.
- متن‌های اصلی فارسی، کوتاه و یکدست شدند.
- منوی اصلی و پنل هلپر خلوت‌تر و خواناتر شدند.

## سازگاری

- دیتابیس قبلی، کاربران، موجودی‌ها و سشن‌ها حذف یا بازنویسی نمی‌شوند.
- تنظیم جدید به‌صورت مهاجرت افزایشی به دیتابیس هر سلف اضافه می‌شود.
- `keep_alive.sh` بدون استفاده از `/dev/fd` باقی مانده و با JailShell
  در cPanel سازگار است.
