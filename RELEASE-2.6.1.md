# نسخه 2.6.1

این نسخه نمایش دستور و رد QuotLyBot را از خروجی فرمان `استیکر` حذف
می‌کند.

## رفتار جدید استیکر

- دستور `استیکر` و `.استیکر` همچنان باید روی پیام هدف ریپلای شوند.
- پیام هدف در یک گفت‌وگوی خصوصی و موقت برای QuotLyBot فرستاده می‌شود.
- پس از دریافت نتیجه، فایل استیکر توسط خود حساب سلف در چت اصلی ارسال
  می‌شود؛ در نتیجه نام، منشن یا پیام QuotLyBot در چت مقصد دیده نمی‌شود.
- پیام فرمان پس از ارسال موفق استیکر حذف می‌شود و فقط استیکر باقی
  می‌ماند.
- پیام‌های موقت ساخته‌شده در گفت‌وگوی خصوصی QuotLyBot نیز پاک می‌شوند.
- اگر ساخت یا ارسال استیکر شکست بخورد، پیام فرمان حذف نمی‌شود و یک خطای
  کوتاه جای آن نمایش داده می‌شود.

## سازگاری

- تنظیمات، دیتابیس، سشن‌ها، موجودی، کارمزد بازی و متن‌های دوست و دشمن
  دست‌نخورده باقی می‌مانند.
- تغییری در `requirements.txt` یا `keep_alive.sh` ایجاد نشده است.
