# نسخه 2.7.0 — فرم‌ساز و منشی قابل‌تنظیم

## فرم‌ساز

- هر سلف می‌تواند چند فرم مستقل با نام، کلمه شروع و ۱ تا ۸ سؤال بسازد.
- فرم‌ساز، متن معرفی و هر فرم از پنل هلپر قابل فعال‌سازی، غیرفعال‌سازی
  و حذف است.
- کاربر با ارسال کلمه شروع فرم وارد پرسش مرحله‌ای می‌شود.
- فرم نیمه‌کاره، مرحله فعلی و پاسخ‌ها در SQLite ذخیره می‌شوند.
- بعد از آخرین سؤال، خلاصه کامل برای تأیید، ویرایش یا لغو نمایش داده
  می‌شود.
- درخواست تأییدشده با شماره، پاسخ‌ها و وضعیت `processing` ذخیره می‌شود.
- نسخه مشتری و نسخه مدیریتی Saved Messages به همان درخواست متصل‌اند.
- ادمین با ریپلای روی نسخه مدیریتی و عبارت‌های فارسی، وضعیت را به
  `processing`، `ready`، `shipped`، `completed` یا `cancelled` تغییر
  می‌دهد.
- وضعیت پیام مشتری و نسخه مدیریتی ویرایش و اعلان جداگانه برای مشتری
  ارسال می‌شود.

## منشی و سؤال‌وجواب

- افزودن سؤال‌وجواب از پنل، کلید پاسخ خودکار را همان لحظه روشن می‌کند.
- سؤال‌ها می‌توانند چند عبارت جایگزین جداشده با `/` داشته باشند.
- موتور تطبیق، علائم نگارشی، فاصله و حروف عربی/فارسی را یکسان می‌کند و
  علاوه بر تطابق کامل، کلیدواژه داخل جمله را نیز تشخیص می‌دهد.
- پاسخ‌های ثابت قبلی منشی حذف شده‌اند.
- کلید `secretary` اکنون پاسخ عمومی قابل‌تنظیم برای پیام بدون تطابق است.
- سؤال‌وجواب، فرم، پاسخ عمومی و حالت آفلاین در پنل کاربرد و اولویت
  مشخص دارند.

## سازگاری

- دیتابیس‌های نسخه‌های قبلی بدون حذف تنظیمات، سشن، موجودی، دوست و دشمن
  مهاجرت می‌کنند.
- فرم‌ساز در نصب‌های قبلی به‌صورت پیش‌فرض خاموش است.
- امکانات نسخه 2.6.1 شامل استیکر بی‌رد، کارمزد شرط‌بندی، دکمه موجودی،
  مدیریت دشمن، Watchdog و سازگاری cPanel حفظ شده‌اند.
- ۳۷ تست خودکار و بررسی همه ۲۰ صفحه پنل موفق بوده است.
