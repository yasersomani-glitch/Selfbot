# انتشار 2.12.0

این نسخه تجربه پنل، راهنمای امکانات، تشخیص حضور و ساعت‌های پروفایل را
به‌صورت افزایشی اصلاح می‌کند. داده‌های نسخه 2.11.0 حذف یا بازنویسی
نمی‌شوند.

## پنل دارای عکس و شناسه

- پنل Inline در صورت دسترسی هلپر به عکس، با تازه‌ترین عکس پروفایل حساب
  ارسال می‌شود.
- نام، یوزرنیم و آیدی عددی سلف‌ساز زیر عکس نمایش داده می‌شوند.
- در صورت نبود عکس یا محدودیت حریم خصوصی، پنل متنی کامل نمایش داده
  می‌شود.
- ویرایش صفحات هم برای پنل متنی و هم برای پنل عکس‌دار پشتیبانی می‌شود.

## دستورهای قابل کپی و توضیح گزینه‌ها

- قطعه‌های محصور در backtick به قالب واقعی Telegram Code تبدیل می‌شوند.
- صفحه‌های راهنمای دستورها دارای دکمه‌های Copy Text هستند.
- پنج صفحه توضیح گزینه‌ها برای امنیت، وضعیت و پروفایل، پیام و
  اتوماسیون، گروه و رابطه‌ها، و ذخیره‌سازی و ظاهر اضافه شده‌اند.
- توضیح «هشدار قبل بلاک» مشخص می‌کند هشدار پیش از بلاک کاربر ناشناس
  ارسال می‌شود و خاموش‌بودن آن باعث بلاک بدون هشدار خواهد شد.

## وضعیت خودکار آنلاین و آفلاین

- وضعیت از رویداد حضور تلگرام، وضعیت گزارش‌شده حساب و فعالیت خروجی
  همان حساب تشخیص داده می‌شود.
- حالت AFK و متن پاسخ آفلاین از ایموجی حضور جدا شده‌اند.
- «همیشه آنلاین» اولویت دارد؛ برای وضعیت واقعی باید خاموش باشد.
- نام اصلی بدون ایموجی ذخیره می‌شود و با خاموش‌شدن قابلیت بازمی‌گردد.

## ساعت نام، بیو و عکس

- بروزرسانی نام و بیو یک اجرای کوتاه دارد و دیگر فرمان را داخل حلقه
  بی‌پایان نگه نمی‌دارد.
- نام خانوادگی و بیوی اصلی پیش از فعال‌سازی ذخیره و پس از خاموش‌شدن
  بازیابی می‌شوند.
- ساعت عقربه‌ای در نخستین چرخه پس از فعال‌سازی بروزرسانی می‌شود.
- فقط یک عکس ساعت‌دار فعال نگه داشته می‌شود؛ عکس ساعت‌دار قبلی حذف
  می‌شود و عکس اصلی محفوظ می‌ماند.

## نصب افزایشی

فایل‌های Python، `requirements.txt` و راهنما را جایگزین کنید. پوشه
`data`، سشن‌ها، `.env` و فایل‌های رمزنگاری را حذف یا جایگزین نکنید.
تنظیمات جدید در اولین اجرا به دیتابیس هر سلف افزوده می‌شوند.
