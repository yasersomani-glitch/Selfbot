# نسخه 2.10.0 — پایداری، امنیت و فرم‌های مرحله‌ای

## پنل و تجربه کاربری

- تمام فرم‌های تنظیمی مرکز مدیریت از ورودی ترکیبی به مراحل جدا تبدیل شد.
- تنظیم سؤال‌وجواب، فرم‌ساز، فیلتر کلمه و کامنت اول در هلپر نیز مرحله‌ای شد.
- افزودن گروهی متن دوست و دشمن، کلمات پاسخ خودکار و آیدی‌های دوست/دشمن
  با قرار دادن هر مورد در یک خط جدا اضافه شد.
- کانال عضویت اجباری همچنان با بررسی کانال، دریافت لینک و تأیید نهایی
  به‌صورت مرحله‌ای ثبت می‌شود.

## مانیتورینگ و نسخه

- داشبورد، تعداد پردازش‌های واقعاً در حال اجرا را از Watchdog جدا نشان می‌دهد.
- توقف ناگهانی و بازیابی موفق سلف برای ادمین‌های دارای دسترسی گزارش می‌شود.
- نسخه، اثرانگشت کد، نسخه قبلی و آخرین زمان آپدیت هر سلف ثبت می‌شود.
- آپدیت تکی و گروهی قبلی حفظ و به تاریخچه انتشار متصل شده است.

## اشتراک و مالی

- اخطار انقضای ۷، ۳ و ۱ روزه و اعلان پایان اعتبار اضافه شد.
- تمدید خودکار از کیف پول پیاده‌سازی شد و در کمبود موجودی به‌شکل امن
  خاموش می‌شود.
- گزارش ۳۰ روزه رسید، درآمد، گردش کیف پول، هزینه روزانه و تمدید اضافه شد.
- گزارش فعالیت مدیر برای تغییرات حساس مقدار قبل و بعد را ثبت می‌کند.

## امنیت

- هر کاربر در هر بازه ۱۵ دقیقه حداکثر سه درخواست کد ورود دارد.
- پس از پنج کد یا رمز نامعتبر، ورود ۳۰ دقیقه قفل می‌شود.
- نشست موقت ورود پس از ۱۰ دقیقه منقضی و کلاینت موقت بسته می‌شود.
- سشن تلگرام با Fernet در فایل و دیتابیس داخلی رمزنگاری می‌شود.
- سشن‌های متنی نسخه‌های قبلی در اولین استفاده خودکار رمزنگاری می‌شوند.
- کلید محلی در `data/.session.key` با دسترسی `600` نگه‌داری می‌شود؛
  می‌توان به‌جای آن `SESSION_ENCRYPTION_KEY` را در محیط تنظیم کرد.
- کد ورود و رمز دومرحله‌ای روی دیسک ذخیره نمی‌شوند.

## سازگاری و آزمون

- مهاجرت SQLite افزایشی است و کاربران، موجودی، تنظیمات و سشن‌ها حذف نمی‌شوند.
- ۷۱ تست قبلی و ۷ تست تازه؛ در مجموع ۷۸ تست موفق.
- بررسی خطاهای نحوی و نام‌های تعریف‌نشده روی تمام سورس و تست‌ها موفق بود.
