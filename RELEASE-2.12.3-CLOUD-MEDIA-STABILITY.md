# Release 2.12.3 — Cloud Media & Stability

## ذخیره‌سازی

- هیچ رسانه کاربری در پوشه‌های هاست ذخیره نمی‌شود.
- ضدحذف و عکس‌های زمان‌دار مستقیماً در Saved Messages کپی می‌شوند.
- بانک ویس و بکاپ عکس پروفایل با شناسه پیام Saved Messages کار می‌کنند.
- رسانه‌های هلپر با Telegram `file_id` نگهداری و فقط در RAM پردازش می‌شوند.
- فایل‌های رسانه قدیمی هنگام اجرای سلف به Saved Messages منتقل و حذف می‌شوند.
- بکاپ ادمین در RAM ساخته و در تلگرام ارسال می‌شود؛ ZIP محلی ندارد.
- `accounts.db` تجمیعی و `data/deleted_selfbots` حذف می‌شوند.

## اصلاح باگ‌ها

- فرمت ISO تاریخ انقضا و Fail-closed شدن خطاها.
- اتصال مجدد همان Telethon client بدون گم‌شدن Handlerها.
- مدیریت و لغو کامل Taskهای پس‌زمینه.
- رزرو/Commit/Refund اتمیک هزینه فعال‌سازی.
- اتمیک و قابل حسابرسی شدن `/kasr`.
- یکتایی شماره تلگرام بین مشتریان.
- بازیابی رزرو نیمه‌تمام پس از Crash.
- قفل `flock` در `keep_alive.sh` و جلوگیری از اجرای دو Main Bot.
- Restart کامل Main، Helper و Self processها.
- وضعیت `uncertain` برای ارسال با نتیجه مبهم و جلوگیری از ارسال دوباره.
- پایان صحیح Futureهای صف ارسال هنگام Shutdown.
- سقف تلاش FloodWait.
- بسته‌شدن قطعی Connectionهای SQLite در Context Manager.
- محدودیت حجم Streaming و محدودیت ابعاد تصاویر.

## نکته

فایل‌های عملیاتی `.env`، `data/users.db`، `data/bot_data_*.db` و سشن‌های رمزگذاری‌شده برای اجرای سرویس ضروری هستند و روی هاست باقی می‌مانند. منظور از Cloud-only، عدم ذخیره فایل رسانه‌ای و خروجی موقت روی هاست است.
