# نسخه 2.10.1 — اصلاح اجرای API و کنترل هلپر

## اصلاحات

- دکمه روشن/خاموش هلپر دوباره در صفحه «تنظیمات» پنل مدیریت نمایش داده
  می‌شود.
- اگر هلپر در تنظیمات فعال باشد اما پردازش آن متوقف شده باشد، وضعیت
  «فعال ولی متوقف» و دکمه راه‌اندازی نمایش داده می‌شود.
- هنگام `FloodWait`، سلف پیام یا ویرایش جایگزین ارسال نمی‌کند تا محدودیت
  Telegram با `SendMessageRequest` جدید طولانی‌تر نشود.
- `MessageNotModifiedError` در حالت متن خطا محسوب نمی‌شود.
- ساخت پنل Inline در زمان `FloodWait` بدون درخواست تکراری متوقف می‌شود.

## نکته نصب روی cPanel

اگر فایل `.venv/bin/pip` بعد از جابه‌جایی پوشه به مسیر قبلی اشاره می‌کند،
بسته‌ها را با این الگو نصب کنید تا shebang خراب `pip` استفاده نشود:

```bash
.venv/bin/python -m pip install -r requirements.txt
```

وابستگی `cryptography` برای رمزنگاری سشن‌ها ضروری است.
