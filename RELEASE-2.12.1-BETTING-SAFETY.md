# نسخه 2.12.1 — اصلاح ایمنی مالی بازی دونفره

این نسخه فقط منطق بازی دونفره و ثبت مالی آن را اصلاح می‌کند و امکانات سلف و پنل هلپر را حذف یا محدود نمی‌کند.

## اصلاحات

- انقضای خودکار بازی‌های بدون حریف و برگشت اتمیک مبلغ سازنده.
- دکمه لغو بازی فقط برای سازنده و برگشت کامل وجه.
- نگهداری تاریخچه بازی‌های تسویه‌شده، لغوشده، منقضی و ناموفق.
- ثبت تمام رزروها، برگشت‌ها، جوایز و کارمزدها در `balance_transactions`.
- واریز کارمزد بازی به حساب مالک ربات.
- گردکردن رو به بالای کارمزد مثبت برای جلوگیری از دورزدن کارمزد در بازی‌های کوچک.
- قفل جداگانه برای هر بازی و حذف انتظار شبکه از تراکنش مالی.
- کنترل سقف مبلغ و تعداد بازی‌های باز هر کاربر.
- بررسی عضویت اجباری برای سازنده و شرکت‌کننده بازی.
- ارسال جایگزین نتیجه در صورت خطای ویرایش پیام و تلاش مجدد خودکار برای نتایج تحویل‌نشده.
- دستور `/gamehistory` برای مشاهده ۱۰ بازی آخر توسط مدیران.
- مهاجرت خودکار دیتابیس‌های قبلی بدون حذف بازی‌ها و موجودی‌ها.

## تنظیمات اختیاری `.env`

```env
BETTING_GAME_TTL_MINUTES=15
BETTING_CLEANUP_INTERVAL=60
BETTING_MAX_STAKE=1000000
BETTING_MAX_OPEN_GAMES_PER_USER=3
BOT_TIMEZONE=Asia/Tehran
```
