# انتشار 2.11.0

این نسخه چهار قابلیت اصلی را به‌صورت افزایشی به نسخه 2.10.1 اضافه
می‌کند و هیچ جدول یا تنظیم قدیمی را حذف نمی‌کند.

## صف ارسال و FloodWait

- ماژول `send_queue.py` همه ارسال‌های خودکار مهم را برای هر حساب
  سریالی می‌کند.
- در `FloodWait` کل صف همان حساب تا پایان زمان اعلامی متوقف می‌شود.
- درخواست محدودشده دوباره در صف قرار می‌گیرد و از بین نمی‌رود.
- حداقل فاصله ارسال پیش‌فرض ۹۰۰ میلی‌ثانیه است.
- عمق صف و زمان مکث در پنل زمان‌بندی نمایش داده می‌شود.

## پاسخ خودکار چندپاسخی و چندرسانه‌ای

- هر قانون می‌تواند تا ۵۰ عبارت محرک و چند پاسخ داشته باشد.
- یکی از پاسخ‌ها به‌صورت تصادفی انتخاب می‌شود.
- متن، عکس، ویدئو، ویس، استیکر، انیمیشن و فایل پشتیبانی می‌شود.
- پاسخ‌های متنی قدیمی جدول `secretary` بدون تغییر همچنان کار می‌کنند.

## زمان‌بندی حرفه‌ای

- برنامه یک‌باره، فاصله‌ای، روزانه و هفتگی.
- محتوای متنی یا رسانه‌ای.
- توقف، ادامه، لغو و حذف خودکار پیام.
- Claim اتمیک برنامه‌های سررسیدشده و بازیابی اجرای نیمه‌کاره.
- ارسال تمام برنامه‌ها از صف مشترک.

## ایموجی آنلاین و آفلاین

- کلید روشن/خاموش مستقل.
- ایموجی آنلاین و آفلاین جداگانه و قابل تنظیم.
- نام اصلی حساب یک‌بار ذخیره می‌شود و با خاموش‌شدن قابلیت بازمی‌گردد.
- «حالت آفلاین» ایموجی آفلاین و وضعیت Offline تلگرام را فعال می‌کند؛
  خروج از آن ایموجی آنلاین را برمی‌گرداند.

## نصب افزایشی

فایل‌های Python و راهنما را روی نسخه قبلی جایگزین کنید و داده‌های پوشه
`data`، سشن‌ها و فایل `.env` را حذف یا جایگزین نکنید. پس از اولین اجرا،
جدول‌ها و تنظیمات جدید خودکار ساخته می‌شوند.
