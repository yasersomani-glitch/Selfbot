# نسخه 2.7.1 — تاس نمایشی تک‌بارمصرف

## تاس نمایشی

- فرمان `تنظیم تاس ۱` تا `تنظیم تاس ۶` نتیجه تاس رسمی بعدی را فقط برای
  همان چت آماده می‌کند.
- تنظیم سه دقیقه اعتبار دارد و بعد از اولین تاس مصرف می‌شود.
- فرمان `تاس ۱` تا `تاس ۶` پرتاب هدف‌دار را مستقیم اجرا می‌کند.
- فرمان `لغو تاس` تنظیم منتظر همان چت را لغو می‌کند.
- پیام فرمان و نتیجه‌های نامطلوب حذف می‌شوند و فقط تاس رسمی هدف باقی
  می‌ماند.
- در صورت محدودیت تلگرام یا نرسیدن به نتیجه، خطا فقط در Saved Messages
  صاحب سلف ثبت می‌شود تا متن کنترلی در چت مقصد باقی نماند.
- پاسخ به پیام و موضوع گروه هنگام جایگزینی تاس حفظ می‌شود.

## مرزبندی با بازی

- تاس نمایشی داخل `self_features.py` و فقط برای پیام خروجی صاحب سلف است.
- این قابلیت به `main_bot.py`، جدول بازی‌ها، کیف پول، کارمزد و انتخاب
  برنده دسترسی ندارد.
- بازی دونفره `بازی ۱۰` همچنان با مولد تصادفی مستقل خود اجرا می‌شود.
- به‌دلیل تولید نتیجه توسط سرور تلگرام، احتمال نمایش لحظه‌ای تلاش‌های
  حذف‌شده یا اعلان آن‌ها وجود دارد.

## سازگاری

- دیتابیس، سشن‌ها، موجودی، فرم‌ها و تنظیمات نسخه 2.7.0 بدون تغییر حفظ
  می‌شوند.
- قابلیت به‌صورت حافظه‌ای و تک‌بارمصرف است و پس از راه‌اندازی مجدد،
  تنظیم منتظر باقی نمی‌ماند.
