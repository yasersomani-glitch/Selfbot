# نسخه 2.7.2 — دوز و تنظیم مستقل تاس/کازینو

## سرگرمی‌های مستقل سلف

- بازی دوز دونفره به `self_features.py` اضافه شد.
- شروع بازی با ریپلای صاحب سلف روی پیام حریف و ارسال `دوز` انجام می‌شود.
- صاحب سلف با ❌ و حریف با ⭕ بازی می‌کند.
- حرکت‌ها با `دوز ۱` تا `دوز ۹` ثبت می‌شوند.
- برد، مساوی، نوبت اشتباه، خانه تکراری و پایان خودکار پس از ۱۵ دقیقه
  بی‌حرکتی کنترل می‌شوند.
- `لغو دوز` فقط بازی فعال همان چت را می‌بندد.

## کازینوی نمایشی

- فرمان `تنظیم کازینو ۱` تا `تنظیم کازینو ۶۴` نتیجه `🎰` بعدی همان چت
  را به‌صورت تک‌بارمصرف آماده می‌کند.
- `تنظیم کازینو جکپات` میان‌بر نتیجه ۶۴ است.
- `کازینو ۱` تا `کازینو ۶۴` یا `کازینو جکپات` اجرای مستقیم را انجام
  می‌دهد.
- `لغو کازینو` تنظیم منتظر همان چت را پاک می‌کند.
- تنظیم پس از سه دقیقه یا اولین کازینوی رسمی مصرف می‌شود.
- خطا یا محدودیت ارسال فقط در Saved Messages صاحب سلف گزارش می‌شود.

## مرزبندی

- دوز و تاس/کازینوی نمایشی فقط در موتور سلف قرار دارند.
- هیچ کد جدیدی به جدول بازی ربات اصلی، موجودی، کارمزد، انتخاب برنده،
  جایزه یا تسویه اضافه نشده است.
- نتیجه `🎲` و `🎰` را سرور تلگرام تولید می‌کند؛ سلف نتیجه‌های نامطلوب
  را حذف و دوباره ارسال می‌کند.
- احتمال دیده‌شدن لحظه‌ای یا اعلان نتیجه حذف‌شده وجود دارد.
- به‌دلیل احتمال پایین بعضی خروجی‌های کازینو، امکان محدودیت ارسال یا
  نرسیدن به نتیجه در سقف تلاش‌ها وجود دارد.

## سازگاری

- دیتابیس، سشن‌ها، فرم‌ها، منشی، موجودی و تنظیمات نسخه 2.7.1 بدون تغییر
  حفظ می‌شوند.
- تنظیم‌های منتظر و بازی دوز در حافظه هستند و پس از ری‌استارت باقی
  نمی‌مانند.
