# نسخه 2.5.0

این نسخه مشکل اجرا نشدن فرمان‌های سلف در گروه‌ها را برطرف می‌کند و
مدیریت متن‌های پاسخ دوست را به پنل اضافه می‌کند.

## اصلاح فرمان‌ها

- پیام‌های خروجی گروه حتی وقتی تلگرام شناسه Send as یا مدیر ناشناس را
  به‌عنوان فرستنده گزارش کند، به‌درستی فرمان شناخته می‌شوند.
- فرمان‌های زمان‌بندی‌شده به‌عنوان فرمان دستی اجرا نمی‌شوند.
- همه فرمان‌های فارسی ابزارها با نقطه و بدون نقطه پذیرفته می‌شوند.
- مسیرهای ویس، ترجمه، دانلود، واترمارک، قفل، فیلتر، ری‌اکت، سکوت، بلاک،
  پروفایل، ضدحذف، قیمت، ارز، اسکرین و اتوماسیون بررسی شده‌اند.
- فرمان‌های پایه پنل، وضعیت، راهنما، تگ، تگ مدیران و اطلاعات در گروه
  با تشخیص جدید پیام خروجی اجرا می‌شوند.

## متن‌های دوست

- جدول جدید `friend_affection_replies` به‌صورت مهاجرت افزایشی ساخته می‌شود.
- هر سلف فهرست متن‌های دلخواه مستقل دارد.
- افزودن و حذف متن‌ها از صفحه «دوست و دشمن ← مدیریت متن‌های دوست» انجام
  می‌شود.
- اگر متن دلخواه وجود داشته باشد، فقط همان متن‌ها استفاده می‌شوند؛ در
  غیر این صورت پاسخ‌های پیش‌فرض نسخه قبل باقی می‌مانند.

## سازگاری و آزمون

- دیتابیس، کاربران، موجودی‌ها، سشن‌ها و تنظیمات قبلی حفظ می‌شوند.
- Watchdog بات اصلی و هلپر تغییر نکرده است.
- `keep_alive.sh` همچنان بدون `/dev/fd` و سازگار با JailShell cPanel است.
- ۲۴ تست خودکار شامل مهاجرت دیتابیس، فرمان‌های گروه، ویس بدون نقطه،
  Send as، متن‌های دوست، پنل، ضدحذف و Watchdog موفق هستند.
