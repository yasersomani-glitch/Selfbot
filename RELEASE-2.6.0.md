# نسخه 2.6.0

این نسخه دستور استیکر QuotLy، کارمزد قابل‌تنظیم بازی، دکمه موجودی و
مدیریت کامل متن‌های دشمن را به خط نسخه 2.5.0 اضافه می‌کند.

## استیکر با QuotLyBot

- دستور `استیکر` و `.استیکر` روی پیام ریپلای‌شده اجرا می‌شود.
- سلف پیام هدف را با اشاره مستقیم به `@QuotLyBot` درخواست می‌کند.
- اگر دستور بدون ریپلای باشد، راهنمای کوتاه نمایش داده می‌شود.
- فرمان پس از ثبت درخواست حذف می‌شود و نتیجه QuotLy در همان گفتگو می‌ماند.

## کارمزد شرط‌بندی

- گزینه «کارمزد شرط‌بندی» به `/admin ← تنظیمات سکه و پرداخت` اضافه شد.
- مقدار مجاز از ۰ تا ۵۰ درصد است و مقدار پیش‌فرض ۰ رفتار نسخه‌های قبلی
  را حفظ می‌کند.
- کارمزد از کل جایزه دو نفر محاسبه می‌شود.
- درصد هنگام ساخت بازی داخل همان بازی ذخیره می‌شود؛ تغییر بعدی ادمین،
  بازی‌های منتظر قبلی را تغییر نمی‌دهد.
- مبلغ کارمزد و جایزه خالص در متن و دکمه‌های نتیجه نمایش داده می‌شوند.

## موجودی

- زیر پاسخ دستور فارسی `موجودی` و `/balance` یک دکمه شیشه‌ای شامل موجودی
  همان کاربر نمایش داده می‌شود.
- فقط صاحب موجودی می‌تواند جزئیات تازه آن دکمه را به‌صورت اعلان ببیند.

## دشمن و متن‌های پاسخ

- دستورهای ریپلای‌شونده `تنظیم دشمن` و `حذف دشمن` اضافه شدند.
- جدول مستقل `enemy_hostile_replies` برای هر سلف به‌صورت خودکار ساخته
  می‌شود.
- صفحه «مدیریت متن‌های دشمن» امکان افزودن، مشاهده و حذف متن‌ها را دارد.
- پاسخ خودکار دشمن از پنل قابل روشن یا خاموش‌کردن است.
- هیچ متن خصمانه پیش‌فرضی وجود ندارد؛ تا وقتی ادمین سلف متنی ثبت نکند،
  پاسخ متنی ارسال نمی‌شود.

## سازگاری و آزمون

- `.env`، `data`، `sessions`، موجودی‌ها، تنظیمات و سشن‌های قبلی حفظ می‌شوند.
- جدول بازی‌های قدیمی با ستون `fee_percent` و مقدار امن صفر مهاجرت می‌کند.
- Watchdog بات اصلی، هلپر و سلف‌ها بدون تغییر باقی مانده است.
- `keep_alive.sh` همچنان بدون `/dev/fd` و سازگار با JailShell cPanel است.
- هر ۳۳ تست خودکار پروژه با موفقیت اجرا شد.
